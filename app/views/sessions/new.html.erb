<% provide(:title, "Log in") %>
<h1>お客様用ログインページ</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_with scope: :session, model: @customer, url: sessions_path, local: true do |f| %>

      <%= f.label :email %>
      <%= f.email_field :email, class: 'form-control' %>

      <%= f.label :password %>
      <%= f.password_field :password, class: 'form-control' %>

      <%= f.submit "ログイン", class: "btn btn-primary" %>
    <% end %>

    <p>アカウント登録がお済みでない場合は下記の方から登録することができます。 <%= link_to "お客様用アカウントページ", '/customers/new' %></p>
  </div>
</div>
